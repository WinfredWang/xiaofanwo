'use strict';

// Register `header` component, along with its associated controller and template
angular.module('header').component('header', {
    templateUrl : 'app/component/header/header.template.html',
    controller :  function FoodRankController() {
    }
});
